# TODO

Актуальные задачи и вопросы, к которым нужно вернуться. Пункты закрываются только после проверки тестами и, когда это уместно, контролируемой проверки на production.

## Ближайшие решения

### Настроить compaction по реальной статистике

- [ ] Накопить не менее 50–100 успешных модельных запросов и хотя бы один достаточно активный диалог двух участников.
- [ ] Собрать только privacy-safe агрегаты без текстов переписки, ответов модели и tool arguments:
  - число пользовательских turns и записей истории;
  - `InputTokens`, `CachedInputTokens`, `OutputTokens`;
  - p50/p90/p95 размера запроса и длительности;
  - частоту compaction;
  - количество и объём сообщений до и после compaction;
  - размер rolling summary и стоимость одного цикла.
- [ ] Проверить, достаточно ли дословного хвоста для сохранения нюансов активного эмоционального разговора.
- [ ] Оценить действующий профиль (`TriggerMessageCount=200`, `RetainRecentMessageCount=80`, `TriggerHistoryCharacters=50000`, `RetainRecentCharacters=20000`) по накопленной статистике и при необходимости скорректировать.
- [ ] Рассмотреть переход от счётчика сообщений к trigger по фактическому размеру model input, например 25–30 тысяч токенов.
- [ ] После выбора обновить конфигурацию, тесты, документацию и production `.env`.

Текущий ориентир по небольшой выборке из 10 production-запросов: вход вырос с 3737 до 7260 токенов, средняя стоимость запроса составила примерно `$0.0009`. Профиль временно увеличен до `200 / 80`; выборка пока недостаточна для окончательной настройки.

### Проверить live-проактивность по реальной хронологии

- [x] `Initiative__Enabled` включён на production после migration rehearsal и отдельного согласования.
- [ ] За первые дни проверить локальный HTML-report без публикации внутренних rationale и proposed messages.
- [ ] Оценить долю `NoAction`, частоту контактов A/B, повторы, suppression, delivery failures и расход токенов.
- [ ] Проверить, что инициативы предлагают конкретный следующий шаг или ограниченный mediation process, а не общий эмоциональный check-in.
- [ ] Отслеживать повторную отправку уже переданного bridge meaning и непрошеные privacy-дисклеймеры.
- [ ] Проверить, что quiet hours 22:00–09:00 МСК, opt-out, временные паузы и лимит двух сообщений за 24 часа работают ожидаемо.
- [ ] После достаточной выборки решить, нужны ли отдельные participant observations и статистика реакции; не вводить психологические ярлыки и единый «relationship score».

### Решить вопрос с голосовыми сообщениями

- [ ] Выбрать cloud speech-to-text или локальное распознавание.
- [ ] Для cloud-варианта сравнить отдельную audio-модель через OpenRouter и специализированный transcription API.
- [ ] До реализации утвердить ограничения длительности и размера аудио.
- [ ] Спроектировать durable обработку: сохранить Telegram `FileId`, отдельно зафиксировать готовую транскрипцию и не оплачивать её повторно после перезапуска.
- [ ] Не хранить исходное аудио после распознавания и не логировать аудио, транскрипцию, `FileId` или URL загрузки с bot token.

## Надёжность и эксплуатация

- [ ] Добавить автоматическое восстановление зависших `PendingDelivery` после аварийного завершения.
- [ ] Определить timeout и нейтральное завершение mediated requests, если адресат не отвечает.
- [ ] Для запуска нескольких экземпляров заменить process-local session lock распределённым lock или lease.
- [ ] Сократить systemd journal на production-сервере и увеличить запас свободного места.

## Правила ведения

- Не добавлять в TODO секреты, Telegram UserId, тексты переписки и другие пользовательские данные.
- Для каждого изменения сначала фиксировать критерий готовности и способ безопасной проверки.
- Коммит, push и deployment выполнять только после отдельного согласования.
